{
  "name": "data-pm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd data-agent-platform && npm install && npm run build",
    "start": "cd data-agent-platform && npm start"
  }
}
