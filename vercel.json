{
  "buildCommand": "cd data-agent-platform && npm install && prisma generate && next build",
  "outputDirectory": "data-agent-platform/.next",
  "installCommand": "cd data-agent-platform && npm install"
}
